# Optional authenticated Ogram management-service base URL.
# The app posts ordered event envelopes to <base>/v1/learning/events with
# browser credentials. When unset and no native preload bridge is present,
# events remain honestly queued in the recoverable local outbox.
# VITE_OGRAM_MANAGEMENT_URL=https://management.example.com
