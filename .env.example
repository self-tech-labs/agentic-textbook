# The browser vertical slice is local-first and needs no secret or API key.
# Future production adapters should use authenticated HttpOnly sessions rather
# than exposing Ogram management credentials to Vite or the browser bundle.
