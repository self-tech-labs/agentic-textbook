# The browser experience is local-first and needs no API key.
# Optional: run the official Gemini-backed WebMCP agent-journey evals.
# Keep the real value only in the ignored .env file or process environment.
GOOGLE_AI=
